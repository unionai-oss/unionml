service: "service:svc"
labels:
   owner: bentoml-integration
   stage: dev
include:
- "*.py"  # A pattern for matching which files to include in the bento
python:
  # TODO: use requirements.txt file
  lock_packages: false  # can remove this in next flytekit release, due to grpcio-status dependency
  packages:
  - pandas
  - requests
  - scikit-learn
  wheels:
  - ./dist/unionml-0.0.0+dev0-py2.py3-none-any.whl
